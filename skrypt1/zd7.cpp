#include <iostream>

int main() {
    int a, b, c;
    float avg;
    
        std::cin >> a >> b >> c;
    
    avg = (a + b + c) / 3.0f;
    
        std::cout << "Srednia arytmetyczna wynosi: " << avg << std::endl;

    return 0;
}