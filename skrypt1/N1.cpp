#include <iostream>

int main() {
    int a, b;
    std::cin >> a >> b;
    if(a == b) {
        std::cout << "liczby sa rowne";
    } else {
        std::cout << "liczby NIE sa rowne";
    }

return 0;
}